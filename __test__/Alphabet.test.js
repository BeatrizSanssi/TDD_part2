import { Alphabet } from '../src/Alphabet'

describe('Alphabet', () => {
  it('should create an instance of the alphabet', () => {
    const alphabet = new Alphabet()
    expect(alphabet).toBeDefined()
  })
})
